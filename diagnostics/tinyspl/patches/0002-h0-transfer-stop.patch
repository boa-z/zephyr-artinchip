--- a/application/baremetal/bootloader/lib/fitimage/fitimage.c
+++ b/application/baremetal/bootloader/lib/fitimage/fitimage.c
@@ -1,3 +1,4 @@
+#include <h0_diag.h>
 /*
  * Copyright (c) 2022-2026, ArtInChip Technology Co., Ltd
  *
@@ -480,10 +481,19 @@
 {
     int rdlen = 0;
     u8 *p = buf;
+    h0_event(1, (uintptr_t)buf, size, offset);
+    if (info->dev_type != DEVICE_SPINAND ||
+        !h0_read_allowed((uintptr_t)buf, size, offset)) {
+        h0_event(9, (uintptr_t)buf, size, offset);
+        h0_event(2, (uintptr_t)buf, size, (uint32_t)-1);
+        return -1;
+    }
 
     //determines whether the read length is 0
-    if (!size)
+    if (!size) {
+        h0_event(2, (uintptr_t)buf, size, 0);
         return size;
+    }
 
     if (info->dev_type == DEVICE_SPINAND || info->dev_type == DEVICE_SPINOR) {
 #if defined(AIC_MTD_BARE_DRV)
@@ -492,8 +502,10 @@
         if (info->dev_type == DEVICE_SPINAND)
             offset = mtd_trans_lga_to_pha(mtd, offset);
 
-        if (offset == UINT32_MAX)
+        if (offset == UINT32_MAX) {
+            h0_event(2, (uintptr_t)buf, size, (uint32_t)-1);
             return -1;
+        }
 
         rdlen = mtd_read(mtd, offset, p, size);
 #endif
@@ -518,6 +530,7 @@
         }
     }
 
+    h0_event(2, (uintptr_t)buf, size, (uint32_t)rdlen);
     return rdlen;
 }
 
@@ -573,6 +586,16 @@
         else
         {
             start_us =  aic_get_time_us();
+#ifndef LPKG_USING_FDTLIB_CRC32_VERIFY
+#error "H0 transfer requires CRC verification"
+#else
+            if (crc1 != 0xf183bd17U) {
+                h0_event(9, load_addr, length, crc1);
+                return -1;
+            }
+#endif
+            h0_state(2);
+            h0_event(3, load_addr, length, offset);
             ret = spl_read(info, offset, (u8 *)load_addr, length);
             show_speed("spl read", length, aic_get_time_us() - start_us);
             if (ret < 0)
@@ -588,6 +611,7 @@
                     printf("APP crc32 error: expect 0x%x, got 0x%x\n", crc1, crc2);
                     return -1;
                 }
+                h0_event(8, load_addr, length, crc2);
                 printf("CRC32 verify OK.\n");
             }
 #else
@@ -636,6 +660,7 @@
 
 int spl_load_simple_fit(struct spl_load_info *info, ulong *entry_point)
 {
+    h0_begin_transfer();
     struct fdt_header *header;
     struct spl_fit_info ctx;
     void *buf = NULL;
@@ -647,7 +672,8 @@
     if (!header)
     {
         printf("No space to malloc for header\n");
-        return -1;
+        ret = -1;
+        goto __exit_header;
     }
 
     /* read itb tree header, to parse itb tree totalsize */
@@ -729,5 +755,8 @@
     aicos_free(MEM_DEFAULT, buf);
 __exit_header:
     aicos_free(MEM_DEFAULT, header);
-    return ret;
-}
+    h0_event(7, 0, 0, (uint32_t)ret);
+    h0_dump(0);
+    printf("H0-TRANSFER-ONLY: FIT attempt ended; no payload jump; return to console\n");
+    return -1;
+}
--- a/application/baremetal/bootloader/lib/common/boot_app.c
+++ b/application/baremetal/bootloader/lib/common/boot_app.c
@@ -1,3 +1,4 @@
+#include <h0_diag.h>
 /*
  * Copyright (c) 2023-2024, ArtInChip Technology Co., Ltd
  *
@@ -37,6 +38,8 @@
     boot_time_show();
     dev = aic_get_boot_device();
     boot_arg = (unsigned long)aic_get_boot_args();
+    printf("H0-TRANSFER-ONLY: boot_app blocked\n");
+    return;
     aicos_dcache_clean();
     aicos_icache_invalid();
     aicos_local_irq_disable();
--- a/bsp/peripheral/spinand/spinand.c
+++ b/bsp/peripheral/spinand/spinand.c
@@ -1,3 +1,4 @@
+#include <h0_diag.h>
 /*
  * Copyright (c) 2024-2026, ArtInChip Technology Co., Ltd
  *
@@ -1019,6 +1020,7 @@
     }
 
     buf = malloc(flash->info->page_size + flash->info->oob_size);
+    h0_event(4, (uintptr_t)buf, flash->info->page_size + flash->info->oob_size, 0);
     if (!buf) {
         pr_err("Failed to malloc spinand buf.\n");
         err = -SPINAND_ERR;
@@ -1098,8 +1100,10 @@
     }
 
 exit_spinand_read:
-    if (buf)
+    if (buf) {
+        h0_event(5, (uintptr_t)buf, 0, 0);
         free(buf);
+    }
     return err;
 }
 
--- a/bsp/artinchip/hal/qspi/hal_qspi_v1x.c
+++ b/bsp/artinchip/hal/qspi/hal_qspi_v1x.c
@@ -1,3 +1,4 @@
+#include <h0_diag.h>
 /*
  * Copyright (c) 2022-2026, ArtInChip Technology Co., Ltd
  *
@@ -730,8 +731,8 @@
     qspi_hw_drop_invalid_data(base, QSPI_DROP_INVALID_DATA);
     qspi_hw_rx_dma_disable(base);
     qspi_hw_tx_dma_disable(base);
-    hal_dma_chan_stop(qspi->dma_rx);
-    hal_dma_chan_stop(qspi->dma_tx);
+    H0_DMA_STOP(qspi->dma_rx);
+    H0_DMA_STOP(qspi->dma_tx);
     aicos_dcache_invalid_range((void *)(unsigned long)t->rx_data, t->data_len);
     if (qspi_master_dynamic_dma(qspi)) {
         hal_release_dma_chan(qspi->dma_tx);
@@ -836,7 +837,7 @@
         }
     tx_stop:
         qspi_hw_tx_dma_disable(base);
-        hal_dma_chan_stop(dma_tx);
+        H0_DMA_STOP(dma_tx);
         if (qspi_master_dynamic_dma(qspi)) {
             hal_release_dma_chan(dma_tx);
             qspi->dma_tx = NULL;
@@ -891,7 +892,7 @@
         }
     rx_stop:
         qspi_hw_rx_dma_disable(base);
-        hal_dma_chan_stop(dma_rx);
+        H0_DMA_STOP(dma_rx);
         aicos_dcache_invalid_range((void *)(unsigned long)t->rx_data, t->data_len);
         if (qspi_master_dynamic_dma(qspi)) {
             hal_release_dma_chan(dma_rx);
@@ -1027,7 +1028,7 @@
 
     base = qspi_hw_index_to_base(qspi->idx);
     qspi_hw_tx_dma_disable(base);
-    hal_dma_chan_stop(dma_tx);
+    H0_DMA_STOP(dma_tx);
     if (qspi_master_dynamic_dma(qspi)) {
         hal_release_dma_chan(dma_tx);
         qspi->dma_tx = NULL;
@@ -1065,7 +1066,7 @@
         len = task->len;
         aicos_dcache_invalid_range((void *)dst, len);
     }
-    hal_dma_chan_stop(dma_rx);
+    H0_DMA_STOP(dma_rx);
     if (qspi_master_dynamic_dma(qspi)) {
         hal_release_dma_chan(dma_rx);
         qspi->dma_rx = NULL;
@@ -1238,12 +1239,12 @@
 out:
     if (ret && qspi_master_dynamic_dma(qspi)) {
         if (qspi->dma_tx) {
-            hal_dma_chan_stop(qspi->dma_tx);
+            H0_DMA_STOP(qspi->dma_tx);
             hal_release_dma_chan(qspi->dma_tx);
             qspi->dma_tx = NULL;
         }
         if (qspi->dma_rx) {
-            hal_dma_chan_stop(qspi->dma_rx);
+            H0_DMA_STOP(qspi->dma_rx);
             hal_release_dma_chan(qspi->dma_rx);
             qspi->dma_rx = NULL;
         }
@@ -1339,7 +1340,7 @@
         qspi_hw_drop_invalid_data(base, QSPI_DROP_INVALID_DATA);
         qspi_hw_tx_dma_disable(base);
         if (qspi->dma_tx) {
-            hal_dma_chan_stop(qspi->dma_tx);
+            H0_DMA_STOP(qspi->dma_tx);
             if (qspi_master_dynamic_dma(qspi)) {
                 hal_release_dma_chan(qspi->dma_tx);
                 qspi->dma_tx = NULL;
@@ -1353,7 +1354,7 @@
 #ifdef AIC_DMA_DRV
         if (qspi->work_mode == QSPI_WORK_MODE_ASYNC_TX_DMA) {
             if (qspi->dma_tx) {
-                hal_dma_chan_stop(qspi->dma_tx);
+                H0_DMA_STOP(qspi->dma_tx);
                 if (qspi_master_dynamic_dma(qspi)) {
                     hal_release_dma_chan(qspi->dma_tx);
                     qspi->dma_tx = NULL;
@@ -1372,7 +1373,7 @@
                 aicos_dcache_invalid_range((void *)dst, len);
             }
             if (dma_rx) {
-                hal_dma_chan_stop(dma_rx);
+                H0_DMA_STOP(dma_rx);
                 if (qspi_master_dynamic_dma(qspi)) {
                     hal_release_dma_chan(dma_rx);
                     qspi->dma_rx = NULL;
--- /dev/null
+++ b/bsp/common/include/h0_diag.h
@@ -0,0 +1,25 @@
+/* SPDX-License-Identifier: Apache-2.0 */
+#ifndef AIC_H0_DIAG_H
+#define AIC_H0_DIAG_H
+#include <stdint.h>
+void h0_begin(void);
+void h0_begin_transfer(void);
+void h0_dma_stop(uintptr_t channel, uint32_t result);
+void h0_event(uint32_t kind, uintptr_t address, uint32_t size, uint32_t result);
+void h0_state(uint32_t stage);
+int h0_dump(uintptr_t entry);
+/* Fixed original-product FIT profile, independently checked against its image.
+ * Numeric interval guard only; not an alias or bus-master ownership proof.
+ */
+static inline int h0_read_allowed(uintptr_t address, uint32_t size, uint32_t offset)
+{
+    if (offset == 0 && (size == 40 || size == 732))
+        return address >= 0x40c80000U && address <= 0x41000000U - size;
+    return address == 0x40000000U && size == 1397820U && offset == 0x800U;
+}
+/* Record an existing stop operation's result; do not add a new stop. */
+#define H0_DMA_STOP(channel) do { \
+    int h0_result = hal_dma_chan_stop(channel); \
+    h0_dma_stop((uintptr_t)(channel), (uint32_t)h0_result); \
+} while (0)
+#endif
--- /dev/null
+++ b/application/baremetal/bootloader/lib/common/h0_diag.c
@@ -0,0 +1,107 @@
+/* SPDX-License-Identifier: Apache-2.0
+ * Original bounded diagnostic recorder. Register facts: docs/provenance.yml.
+ * Offline diagnostic SPL only; not a hardware safety or recovery assertion.
+ */
+#include <stdint.h>
+#include <stdio.h>
+#include <h0_diag.h>
+
+struct event { uint32_t kind, address, size, result; };
+static struct event events[256];
+static unsigned int used, dropped, active;
+static unsigned int summarize_dma;
+static uint32_t irq_lock(void)
+{
+    uint32_t state;
+    __asm__ volatile ("csrrci %0, mstatus, 8" : "=r"(state) :: "memory");
+    return state;
+}
+static void irq_restore(uint32_t state)
+{
+    if (state & 8)
+        __asm__ volatile ("csrsi mstatus, 8" ::: "memory");
+}
+void h0_event(uint32_t kind, uintptr_t address, uint32_t size, uint32_t result)
+{
+    uint32_t state = irq_lock();
+    if (active) {
+        if (used < sizeof(events) / sizeof(events[0]))
+            events[used++] = (struct event){kind, address, size, result};
+        else if (dropped != ~0U)
+            dropped++;
+    }
+    irq_restore(state);
+}
+static uint32_t reg_read(uintptr_t address)
+{
+    return *(volatile uint32_t *)address;
+}
+void h0_dma_stop(uintptr_t channel, uint32_t result)
+{
+    uint32_t state = irq_lock();
+    if (active && summarize_dma) {
+        /* Summarize only the contiguous stop run. Never cross a read,
+         * allocation, free or snapshot boundary. First occurrence order is
+         * retained, but ordering within the run is deliberately not evidence.
+         */
+        for (unsigned int i = used; i > 0 && events[i - 1].kind == 16; i--) {
+            struct event *event = &events[i - 1];
+            if (event->address == channel && event->result == result) {
+                if (event->size != ~0U)
+                    event->size++;
+                else if (dropped != ~0U)
+                    dropped++;
+                irq_restore(state);
+                return;
+            }
+        }
+        h0_event(16, channel, 1, result);
+    } else {
+        h0_event(6, channel, 0, result);
+    }
+    irq_restore(state);
+}
+void h0_state(uint32_t stage)
+{
+    uint32_t state = irq_lock(), mie, mtvec, cache;
+    __asm__ volatile ("csrr %0, mie" : "=r"(mie));
+    __asm__ volatile ("csrr %0, mtvec" : "=r"(mtvec));
+    __asm__ volatile ("csrr %0, 0x7c1" : "=r"(cache));
+    h0_event(10, stage, state, mie);
+    h0_event(11, stage, mtvec, cache);
+    /* Source-defined SYSCFG TCM/SRAM configuration and SYSMAP pairs.
+     * Readback is raw: no claim that these exhaust all aliases or bus masters.
+     */
+    h0_event(12, 0x18000160, stage, reg_read(0x18000160));
+    for (unsigned int i = 0; i < 16; i++)
+        h0_event(13, 0x2ffff000 + i * 4, stage, reg_read(0x2ffff000 + i * 4));
+    for (unsigned int i = 0; i < 8; i++)
+        h0_event(14, 0x10000100 + i * 0x40, stage, reg_read(0x10000100 + i * 0x40));
+    irq_restore(state);
+}
+static void begin(unsigned int transfer)
+{
+    uint32_t state = irq_lock();
+    used = dropped = 0;
+    active = 1;
+    summarize_dma = transfer;
+    irq_restore(state);
+    h0_state(1);
+    printf("H0-SPL begin schema=%u instrumentation=active acceptance=pending\n", transfer ? 2U : 1U);
+}
+void h0_begin(void) { begin(0); }
+void h0_begin_transfer(void) { begin(1); }
+int h0_dump(uintptr_t entry)
+{
+    h0_state(3);
+    uint32_t state = irq_lock();
+    active = 0;
+    irq_restore(state);
+    printf("H0-SPL entry=%08lx count=%u dropped=%u\n", (unsigned long)entry, used, dropped);
+    for (unsigned int i = 0; i < used; i++)
+        printf("H0-E %u %u %08lx %08lx %08lx\n", i, (unsigned int)events[i].kind,
+               (unsigned long)events[i].address, (unsigned long)events[i].size,
+               (unsigned long)events[i].result);
+    printf("H0-SPL end evidence=%s hardware=pending\n", dropped ? "INCOMPLETE" : "CAPTURED");
+    return dropped ? -1 : 0;
+}
--- a/target/configs/d13x_d50t-2-lite_baremetal_bootloader_defconfig
+++ b/target/configs/d13x_d50t-2-lite_baremetal_bootloader_defconfig
@@ -75,3 +75,5 @@
 CONFIG_LPKG_CHERRYUSB_OTG_MANUAL=y
 CONFIG_LPKG_CHERRYUSB_OTG_DEF_DEVICE=y
 CONFIG_AIC_PANEL_CUSTOM_RESOLUTION=y
+
+# CONFIG_AIC_BOOTLOADER_CMD_MEM is not set
