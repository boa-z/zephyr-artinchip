# Short coverage profile only: it proves the application starts, every entity
# runs, counters progress and the PASS/FAIL state machine works. It is never
# recorded as the ten-minute hardware stress result.
CONFIG_AIC_Z0_STRESS_DURATION_SEC=3
CONFIG_AIC_Z0_STRESS_HEARTBEAT_SEC=1
