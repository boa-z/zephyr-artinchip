# SPDX-License-Identifier: Apache-2.0
# Experimental board profile, not a new default or inferred peripheral map.
# The 144-slot CLIC table the packager verifies is the SoC default
# (soc/artinchip/d13x/Kconfig.defconfig), so it is not repeated here.
CONFIG_ARTINCHIP_D13X_BOOT_CONTRACT=y
